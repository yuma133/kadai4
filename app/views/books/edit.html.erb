<% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
<% end %>
<div class="row">
	<div class="col-xs-9">
		<h1 class="text-center">Editing Book</h1>
		<%= form_for(@book) do |b| %>
		<h4>Title</h4>
		<%= b.text_field :title , class: "edit-btn" %>
		<h4>Opinion</h4>
		<%= b.text_area :body, class: "edit-btn" %>
		<%= b.submit "Update Book", class: "btn btn-primary edit-btn" %>
		<% end %>
		<%= link_to "Show", book_path(@book.id) %>
    	<p class="edit-1">|</p>
    	<%= link_to "Back", books_path %>
	</div>
</div>